# TenzoRSE
header only c++17 string obfuscation. hides from IDA, x64dbg, strings cmd. decodes char by char so full plaintext never sits in memory. compile time shuffle plus xor plus bit rotate. seed changes every build. no heap, no deps.
